<div class="m-4">

  <%= render 'resume' %>

  <div class="flex flex-wrap gap-4 mt-4">
    <%= render 'recent_events' %>
    <div class="flex flex-col flex-wrap gap-4">
      <%= render 'events_by_status' %>
      <%= render 'institutions' %>
    </div>
    <%= render 'resources_by_status' %>
  </div>

</div>
